import 'package:flutter/material.dart';

class AdmHome extends StatelessWidget {
  const AdmHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Adm Home'),
    );
  }
}
