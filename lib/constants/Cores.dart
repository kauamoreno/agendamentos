import 'package:flutter/material.dart';

class Cores {

  // Vermelho
  static const Color red = Color.fromRGBO(220, 53, 69, 1.0);
  static const Color redDanger = Color.fromRGBO(227, 6, 19, 1.0);

  // Claro
  static const Color white = Color.fromRGBO(255, 255, 255, 1.0);
  static const Color light = Color.fromRGBO(248, 249, 250, 1.0);

  // Escuro
  static const Color gray = Color.fromRGBO(108, 117, 125, 1.0);
  static const Color black = Color.fromRGBO(51, 51, 51, 1.0);
  
} 
